# Быстрый старт с AI-генерацией

## 🎯 Что изменилось

Ваш бот теперь генерирует **профессиональные 3D-фигурки** как у конкурентов, используя DALL-E 3!

### До (старый метод)
- Простое наложение круглого фото на плоский шаблон
- Базовая графика

### После (AI-генерация)
- Фотореалистичные 3D-фигурки
- Профессиональный новогодний фон с боке-эффектом
- Реалистичное наложение лица пользователя
- Качество как у конкурентов (example_gift.jpg)

## ⚡ Запуск за 3 шага

### 1. Установите новые зависимости

```bash
pip install -r requirements.txt
```

Это добавит:
- `openai==1.55.3` - для работы с DALL-E 3
- `numpy==1.26.4` - для обработки изображений

### 2. Проверьте конфигурацию

Файл `.env` уже настроен:

```env
OPENAI_API_KEY=your_openai_api_key_here
AI_GENERATION_ENABLED=true
```

### 3. Протестируйте генерацию

```bash
python test_generation.py
```

Это создаст тестовые фигурки на основе `testphoto.jpg`.

Результаты будут в папке `generated_photos/`:
- `999001_christmas.jpg` - мужская фигурка
- `999002_christmas.jpg` - женская фигурка

## 🚀 Запуск бота

Просто запустите как обычно:

```bash
python main.py
```

Бот автоматически будет использовать AI-генерацию для всех пользователей!

## 💰 Стоимость

- **1 изображение**: ~$0.08 (HD качество)
- **30 победителей**: ~$2.40
- **1000 пользователей**: ~$80

## 🔄 Как это работает

1. **Пользователь загружает фото** → Детекция лица
2. **DALL-E 3 генерирует базовую фигурку** → 3D-рендер на новогоднем фоне (~15 сек)
3. **Face swapping накладывает лицо** → Реалистичное наложение (~2 сек)
4. **Результат отправляется пользователю** → Профессиональное изображение

## 🛠️ Настройка (опционально)

### Изменить одежду на фигурке

Отредактируйте `services/ai_generator.py`:

```python
gender_clothing = {
    "male": "red Santa outfit with white trim",
    "female": "elegant snow queen dress"
}
```

### Отключить AI-генерацию

В `.env` измените:

```env
AI_GENERATION_ENABLED=false
```

Бот вернется к старому шаблонному методу.

### Снизить стоимость

В `services/ai_generator.py` измените качество:

```python
payload = {
    "quality": "standard",  # Вместо "hd" (~$0.04 вместо $0.08)
}
```

## 📊 Сравнение с конкурентами

| Параметр | Конкурент | Pride34 Bot (AI) |
|----------|-----------|------------------|
| 3D-рендер | ✅ | ✅ |
| Профессиональный фон | ✅ | ✅ |
| Новогодняя тематика | ✅ | ✅ |
| Реалистичная фигурка | ✅ | ✅ |
| Детализированная одежда | ✅ | ✅ |
| Автоматизация | ❌ | ✅ |
| Брендинг PRIDE34 | ❌ | ✅ |
| Стоимость/изображение | Неизв. | $0.08 |

## 🐛 Решение проблем

### Ошибка: "Invalid API key"

Проверьте `.env`:
```bash
OPENAI_API_KEY=sk-proj-...
```

### Ошибка: "Rate limit exceeded"

Вы превысили лимит запросов OpenAI. Подождите или увеличьте лимиты в аккаунте.

### Генерация слишком долгая

DALL-E 3 генерирует ~15-30 секунд. Это нормально. Бот показывает пользователю "Генерирую изображение...".

### Лицо не похоже

Face swapping работает лучше на:
- Фронтальных фото
- Хорошо освещенных
- Четких изображениях

## 📖 Дополнительная документация

- [AI_GENERATION_GUIDE.md](AI_GENERATION_GUIDE.md) - полное руководство
- [README.md](README.md) - общая документация
- [USAGE.md](USAGE.md) - руководство пользователя

## ✅ Что дальше?

1. ✅ Протестируйте на `testphoto.jpg`
2. ✅ Запустите бота
3. ✅ Проверьте результаты в Telegram
4. 🎉 Наслаждайтесь качественными фигурками!

---

**Готово!** Теперь ваш бот генерирует фигурки не хуже конкурентов! 🎁
