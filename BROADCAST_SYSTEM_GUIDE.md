# Руководство по расширенной системе рассылки и двусторонней коммуникации

## Обзор

Система предоставляет мощные инструменты для таргетированной рассылки сообщений пользователям и двусторонней коммуникации через форум Telegram.

## Основные возможности

### 1. Таргетированная рассылка

Система позволяет отправлять сообщения различным группам пользователей:

#### Доступные группы:

- **👥 Все пользователи** - рассылка всем зарегистрированным пользователям
- **👨 Мужчины** - только пользователи мужского пола
- **👩 Женщины** - только пользователи женского пола
- **✅ Получили открытку** - пользователи, завершившие квиз и получившие новогоднюю фигурку
- **⏳ Не дошли до открытки** - пользователи, которые не завершили квиз
- **👤 Персональная рассылка** - отправка сообщения конкретному пользователю по ID
- **🧪 Тест (только админы)** - тестовая рассылка только администраторам

### 2. Превью получателей

Перед отправкой рассылки система показывает:
- Полный список получателей (с пагинацией по 10 человек)
- Пол пользователя (👨/👩)
- Статус прохождения квиза (✅/⏳)
- Ссылки на топики форума (если есть)
- Общее количество получателей

### 3. Поддерживаемые типы контента

Рассылка поддерживает:
- Текстовые сообщения с HTML-форматированием
- Фото с подписью
- Видео с подписью
- Документы

## Как использовать систему рассылки

### Шаг 1: Открыть админ-панель

Отправьте команду `/admin` или нажмите кнопку "Отправить рассылку" в админ-панели.

### Шаг 2: Выбрать группу получателей

Выберите одну из доступных групп:

**Для группового выбора:**
1. Нажмите на нужную группу (например, "👨 Мужчины")
2. Система покажет превью пользователей этой группы
3. Пролистайте страницы, чтобы просмотреть всех получателей

**Для персональной рассылки:**
1. Нажмите "👤 Персонально (по ID)"
2. Введите Telegram ID пользователя (например: `123456789`)
3. Система проверит существование пользователя

### Шаг 3: Просмотр получателей

На экране превью вы увидите:
```
👨 Мужчины
Страница 1/3

📊 Всего получателей: 25

👨✅ Иван Петров
👨⏳ Алексей Сидоров
...
```

Доступные действия:
- **◀️ / ▶️** - навигация по страницам
- **✏️ Написать сообщение** - перейти к созданию сообщения
- **🔙 Выбрать другую группу** - вернуться к выбору группы
- **❌ Отмена** - отменить рассылку

### Шаг 4: Создание сообщения

После нажатия "✏️ Написать сообщение":

1. Отправьте ваше сообщение боту
2. Можно использовать:
   - Текст с HTML-тегами: `<b>жирный</b>`, `<i>курсив</i>`, `<code>код</code>`
   - Фото (просто отправьте фото с подписью)
   - Видео (отправьте видео с подписью)
   - Документы

### Шаг 5: Подтверждение и отправка

1. Система покажет подтверждение с количеством получателей
2. Нажмите **✅ Отправить** для начала рассылки
3. Следите за прогрессом отправки в реальном времени

### Шаг 6: Результаты

После завершения вы увидите статистику:
```
✅ Рассылка завершена!

📊 Статистика:
• Всего пользователей: 25
• Успешно отправлено: 24
• Ошибок: 1
• Процент доставки: 96.0%
```

## Двусторонняя коммуникация через форум

### Как это работает

Каждый пользователь, завершивший квиз, автоматически получает свой топик в форуме группы. Через этот топик возможна двусторонняя коммуникация.

### Админ → Пользователь

1. Откройте форум группы Pride34
2. Найдите топик нужного пользователя (по имени или Pride GIFT ID)
3. Напишите сообщение в топик
4. **Бот автоматически перешлёт** ваше сообщение пользователю в личные сообщения

### Пользователь → Админ

1. Пользователь пишет боту в личные сообщения
2. Если у пользователя есть топик форума, **бот автоматически переслёт** сообщение в его топик
3. Администраторы увидят сообщение в форуме и смогут ответить

### Что пересылается

- Текстовые сообщения
- Фото
- Видео
- Документы
- Голосовые сообщения
- Стикеры

### История переписки

Все сообщения логируются в базу данных в таблицу `user_messages`:
- `user_id` - ID пользователя
- `forum_message_id` - ID сообщения в форуме
- `user_message_id` - ID сообщения в личном чате
- `direction` - направление ('to_user' или 'from_user')
- `created_at` - время отправки

## Технические детали

### База данных

#### Таблица `users`
- `quiz_completed` (BOOLEAN) - пользователь получил открытку
- `forum_topic_id` (INTEGER) - ID топика форума для двусторонней связи

#### Таблица `user_messages` (новая)
```sql
CREATE TABLE user_messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id BIGINT NOT NULL,
    forum_message_id INTEGER NOT NULL,
    user_message_id INTEGER,
    direction VARCHAR(20) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Фильтры пользователей

Реализованы в `UserCRUD.get_users_by_filter()`:

```python
# Примеры использования
users_male = await UserCRUD.get_users_by_filter(session, 'male')
users_completed = await UserCRUD.get_users_by_filter(session, 'completed')
all_users = await UserCRUD.get_users_by_filter(session, 'all')
```

### Безопасность

- Все обработчики проверяют `is_admin()` для админских функций
- Пользовательские сообщения не могут попасть в чужие топики
- Валидация ID при персональной рассылке
- Бот не пересылает собственные сообщения (защита отループов)

### Производительность

- Пагинация при показе больших групп (10 пользователей на страницу)
- Сохранение только ID в state, загрузка деталей по требованию
- Anti-flood задержка 0.05с при рассылке
- Обновление статистики каждые 10 отправленных сообщений

## Примеры использования

### Пример 1: Рассылка поздравления женщинам

1. `/admin` → "Отправить рассылку"
2. Выбрать "👩 Женщины"
3. Просмотреть список (например, 47 получательниц)
4. Нажать "✏️ Написать сообщение"
5. Отправить:
   ```
   С 8 марта! 🌸

   Желаем вам радости, здоровья и новых побед!

   Специальное предложение: скидка 30% на все абонементы по промокоду MARCH8
   ```
6. Подтвердить отправку

### Пример 2: Напоминание незавершившим квиз

1. `/admin` → "Отправить рассылку"
2. Выбрать "⏳ Не дошли до открытки"
3. Просмотреть список
4. Написать сообщение с напоминанием
5. Отправить

### Пример 3: Персональное сообщение

1. `/admin` → "Отправить рассылку"
2. Выбрать "👤 Персонально (по ID)"
3. Ввести ID: `987654321`
4. Отправить персональное сообщение

### Пример 4: Тестовая рассылка

1. `/admin` → "Отправить рассылку"
2. Выбрать "🧪 Тест (только админы)"
3. Протестировать сообщение на себе
4. Затем отправить реальную рассылку

## Решение проблем

### Сообщения не пересылаются в форум

1. Проверьте, что бот добавлен в форум группу
2. Убедитесь, что у пользователя есть `forum_topic_id` в базе
3. Проверьте настройку `FORUM_GROUP_ID` в .env

### Сообщения от админа не доходят до пользователя

1. Убедитесь, что вы пишете в топик пользователя (не в общий чат)
2. Проверьте, что пользователь не заблокировал бота
3. Посмотрите логи на наличие ошибок

### Рассылка не отправляется

1. Проверьте права администратора
2. Убедитесь, что в выбранной группе есть пользователи
3. Проверьте формат сообщения (HTML должен быть корректным)

## Команды для миграции

Перед использованием новой системы выполните миграцию:

```bash
# Создать таблицу user_messages
python migrate_add_user_messages.py
```

## Дополнительная информация

- Все действия логируются в файл logs/
- Статистика рассылок сохраняется в логах
- История сообщений хранится в БД бессрочно
- Для очистки истории можно использовать SQL-запросы

## Поддержка

При возникновении вопросов или проблем:
1. Проверьте логи бота
2. Убедитесь в правильности настроек в .env
3. Проверьте права бота в форуме
