# Руководство по редактированию текстов бота

## Обзор

Система редактирования текстов позволяет администраторам изменять все сообщения бота через удобный интерфейс в Telegram, без необходимости редактировать код.

## Как использовать

### 1. Вход в меню редактирования

1. Откройте админ-панель бота (команда для админов)
2. Нажмите кнопку **"Редактировать тексты"**
3. Откроется меню с категориями текстов

### 2. Категории текстов

#### 👋 Приветствие
- Текст приветственного сообщения при команде `/start`
- Отображается вместе с картинкой `welcome.jpg`

#### ❓ Вопросы квиза
- 5 вопросов квиза
- По 4 варианта ответа на каждый вопрос
- Всего 25 редактируемых элементов (5 вопросов × 5 элементов)

#### 👤 Выбор пола
- Текст сообщения, предлагающего выбрать пол
- Отображается после завершения квиза

#### 📸 Запрос фото
- Текст сообщения с просьбой загрузить фото
- Отображается после выбора пола

#### 🔮 Предсказания
- 5 вариантов предсказаний на основе ответов квиза:
  - `fitness_enthusiast` - Фитнес-энтузиаст
  - `balanced_lifestyle` - Сбалансированный подход
  - `sweet_lover` - Любитель сладкого
  - `beginner` - Новичок
  - `default` - По умолчанию

#### 🔘 Кнопки
- Текст кнопки "Я готов!"
- Текст кнопки "Мужской"
- Текст кнопки "Женский"

### 3. Процесс редактирования

1. **Выберите категорию** - нажмите на нужную категорию
2. **Выберите элемент** - если в категории несколько элементов (например, вопросы квиза)
3. **Просмотрите текущий текст** - система покажет текущее значение
4. **Отправьте новый текст** - просто отправьте сообщение с новым текстом
5. **Подтверждение** - система сохранит изменения и покажет подтверждение

### 4. Навигация

В любой момент вы можете:
- **◀️ Назад** - вернуться к предыдущему меню
- **❌ Отмена** - отменить редактирование и вернуться к категориям
- **◀️ Назад в админку** - выйти из редактора в главное меню админки

## Технические детали

### Хранение текстов

Все тексты хранятся в файле `bot/texts.json` в формате JSON:

```json
{
  "welcome": {
    "text": "Текст приветствия...",
    "description": "Описание"
  },
  "quiz": {
    "question_1": {
      "text": "Текст вопроса...",
      "options": ["Вариант 1", "Вариант 2", ...]
    }
  }
}
```

### Применение изменений

- Изменения **вступают в силу немедленно** для новых пользователей
- Пользователи, которые уже находятся в процессе прохождения квиза, могут видеть старые тексты до перезагрузки бота
- Для гарантированного применения изменений рекомендуется перезапустить бота

### Безопасность

- Доступ к редактированию имеют только администраторы (ID указаны в `ADMIN_IDS`)
- Все изменения логируются
- Рекомендуется создать резервную копию `texts.json` перед массовыми изменениями

## Форматирование текста

Вы можете использовать HTML-теги для форматирования:

- `<b>текст</b>` - **жирный текст**
- `<i>текст</i>` - *курсив*
- `<code>текст</code>` - `моноширинный текст`
- `\n\n` - перенос строки (пустая строка между абзацами)

Пример:
```
<b>Добро пожаловать!</b>

Это новый абзац с <i>курсивом</i>.
```

## Резервное копирование

Рекомендуется регулярно создавать резервные копии файла `texts.json`:

```bash
cp bot/texts.json bot/texts.json.backup
```

## Восстановление из резервной копии

Если что-то пошло не так:

```bash
cp bot/texts.json.backup bot/texts.json
```

Затем перезапустите бота.

## Модули системы

1. **bot/texts.json** - JSON-файл с текстами
2. **bot/texts.py** - TextManager для управления текстами
3. **handlers/text_editor.py** - обработчики редактирования
4. **bot/states.py** - состояния FSM для редактирования
5. **bot/keyboards.py** - клавиатуры для навигации

## Примеры использования

### Изменить приветственное сообщение

1. Админ-панель → Редактировать тексты
2. Выбрать "👋 Приветствие"
3. Увидеть текущий текст
4. Отправить новый текст
5. Получить подтверждение

### Изменить вопрос квиза

1. Админ-панель → Редактировать тексты
2. Выбрать "❓ Вопросы квиза"
3. Выбрать нужный вопрос (например, "Вопрос 1")
4. Выбрать что редактировать (текст вопроса или вариант ответа)
5. Отправить новый текст
6. Получить подтверждение

## Устранение неполадок

### Изменения не применяются

1. Проверьте, что вы администратор (ваш ID в `ADMIN_IDS`)
2. Перезапустите бота для гарантированного применения
3. Проверьте файл `texts.json` на корректность JSON

### Ошибка при сохранении

1. Проверьте права доступа к файлу `texts.json`
2. Проверьте, что диск не заполнен
3. Проверьте логи бота на наличие ошибок

### Текст слишком длинный

Telegram имеет ограничения:
- Максимум 4096 символов для текстового сообщения
- Максимум 1024 символа для caption к фото

Если текст слишком длинный, разбейте его на несколько сообщений или сократите.

---

**Создано:** 2025-12-28
**Версия:** 1.0
